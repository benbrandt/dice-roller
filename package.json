{
  "devDependencies": {
    "serverless": "^1.66.0"
  },
  "name": "dice-roller",
  "scripts": {
    "deploy": "serverless deploy",
    "build:js": "ncc build handler.js -o handler-dist"
  },
  "dependencies": {
    "@sentry/node": "^5.14.2",
    "@zeit/ncc": "^0.21.1",
    "botbuilder": "^3.16.0",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.8"
  }
}
