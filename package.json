{
  "devDependencies": {
    "serverless": "^1.60.4"
  },
  "name": "dice-roller",
  "scripts": {
    "deploy": "serverless deploy",
    "build:js": "ncc build handler.js -o handler-dist"
  },
  "dependencies": {
    "@sentry/node": "^5.10.2",
    "@zeit/ncc": "^0.20.5",
    "botbuilder": "^3.16.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.8"
  }
}
