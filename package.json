{
  "devDependencies": {
    "serverless": "^1.64.0"
  },
  "name": "dice-roller",
  "scripts": {
    "deploy": "serverless deploy",
    "build:js": "ncc build handler.js -o handler-dist"
  },
  "dependencies": {
    "@sentry/node": "^5.12.4",
    "@zeit/ncc": "^0.21.1",
    "botbuilder": "^3.16.0",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.8"
  }
}
