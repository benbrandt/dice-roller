{
  "devDependencies": {
    "serverless": "^1.41.0",
    "serverless-rust": "^0.3.3"
  },
  "name": "dice-roller",
  "scripts": {
    "deploy": "serverless deploy",
    "build:js": "ncc build handler.js -o handler-dist"
  },
  "dependencies": {
    "@sentry/node": "^5.1.0",
    "@zeit/ncc": "^0.18.1",
    "botbuilder": "^3.16.0",
    "got": "^9.6.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7"
  }
}
