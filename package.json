{
  "devDependencies": {
    "serverless": "^1.61.0"
  },
  "name": "dice-roller",
  "scripts": {
    "deploy": "serverless deploy",
    "build:js": "ncc build handler.js -o handler-dist"
  },
  "dependencies": {
    "@sentry/node": "^5.11.0",
    "@zeit/ncc": "^0.20.5",
    "botbuilder": "^3.16.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.8"
  }
}
