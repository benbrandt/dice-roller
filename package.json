{
  "devDependencies": {
    "serverless": "^1.37.1",
    "serverless-rust": "^0.2.1"
  },
  "name": "dice-roller",
  "scripts": {
    "deploy": "serverless deploy",
    "build:js": "ncc build handler.js -o handler-dist"
  },
  "dependencies": {
    "@zeit/ncc": "^0.14.0",
    "botbuilder": "^3.16.0",
    "got": "^9.6.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5"
  }
}
